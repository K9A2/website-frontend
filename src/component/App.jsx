import React, {Component} from 'react';

export default class App extends Component {

  /**
   * 返回当前组件的 DOM 结构
   * @return {*} 当前组件的 DOM 结构
   */
  render() {
    return (
      <h1>test</h1>
    );
  }
}
